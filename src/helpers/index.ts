import _hashPassword from "./hashPassword";
import _verificationFiltering from "./verificationFiltering";
namespace Helper {
  export const hashPassword = _hashPassword;
    export const verificationFiltering = _verificationFiltering;
}

export default Helper;
